---
// src/components/ui/card-description.astro
import { cn } from "../../lib/utils";

interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...rest } = Astro.props;
const classList = cn("text-sm text-muted-foreground", className);
---

<p class={classList} {...rest}>
  <slot />
</p>