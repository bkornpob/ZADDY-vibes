---
// src/components/ui/card-header.astro
import { cn } from "../../lib/utils";

interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...rest } = Astro.props;
const classList = cn("flex flex-col space-y-1.5 p-6", className);
---

<div class={classList} {...rest}>
  <slot />
</div>